version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      # Define your OpenAI API key here or use .env file
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    restart: always
